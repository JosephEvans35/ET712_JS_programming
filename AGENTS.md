# Agent Instructions

## Project Context

- This is a beginner JavaScript programming coursework workspace organized by class labs and homework folders.
- Preserve the existing lesson progression: HTML/CSS foundations, arrays and conditionals, loops, then functions and interactive browser behavior.
- The current learning path points from `classlab7` and `classlab8` toward `Homework2`, whose stated topics are arrays, functions, and an AI assistant.

## How To Anticipate The Next Step

- Read the active file and its nearest preceding lab before proposing changes.
- Infer the next lesson-sized concept from the code already being written. For example, after arrays and loops, suggest a small function that processes an array or connects a prompt/input to a result.
- State the prediction briefly and cite the evidence in the nearby files. Treat it as a hypothesis, not an instruction to invent requirements.
- Before making a larger assignment solution, ask whether the user wants an explanation, a small example, or a complete implementation. Prefer a minimal example that matches the current lesson.
- Keep the user's naming, formatting style, and simple JavaScript constructs unless correcting a clear error. Explain newer syntax when introducing it.

## Working Conventions

- Keep each lab or homework self-contained in its existing folder; do not reorganize coursework files.
- For browser exercises, check that the HTML loads the intended script and use the browser console for output unless the assignment asks for visible page output.
- For JavaScript exercises, validate syntax and behavior with the smallest available check, such as running the script with Node when it does not depend on `prompt` or DOM APIs, or opening the corresponding HTML file in a browser.
- Avoid adding frameworks, build tooling, or dependencies to these introductory exercises unless the user explicitly requests them.
- Do not rewrite unrelated examples or polish spelling and formatting unrelated to the requested learning objective.

## Useful Examples

- `classlab7/class7.js`: arrays, array methods, conditionals, and user input.
- `classlab8/scriptclass8.js`: `for` and `while` loops, validation, and `continue`.
- `Homework1/index.html`: an earlier interactive page with a small inline assistant function.
- `Homework2/evans.js` and `Homework2/evans.html`: the current homework starting point.
